# Первый GPU-замер стоимости: угадыватель + акустический пруннер на RTX 4090

Дата: 31.08.2026. Под RunPod `n3uk2ynkko08s9`, RTX 4090 24 ГБ, драйвер 580.178.04,
образ `runpod/pytorch:2.8.0-py3.11-cuda12.8.1-cudnn-devel-ubuntu22.04`.
Скрипт: `scripts/scorer-cost-bench.py`, 30 итераций на фазу, синтетический тон
вместо речи, кандидаты-заглушки. **Это замер стоимости, не корректности** —
recall/retention требуют размеченного корпуса, которого ещё нет
(см. `scripts/acoustic-replay-bench.py`).

Сырые артефакты: `results/gpu-4090/` в этой ветке
(`scorer-cost-ru.json`, `scorer-cost-he.json`, `stage1-think-*.json`,
`pod-log-n3uk2.txt`).

## Числа

### Акустический пруннер (wav2vec2 CTC, float16)

| | RU (base) | HE (large-xlsr-53) |
|---|---|---|
| inference p50, любое окно 50–250 мс | **6–7 мс** | **11–13 мс** |
| inference p95 | ≤ 7.7 мс | ≤ 13.9 мс |
| очередь p95 при in-flight 2 | 6 мс | 12 мс |
| очередь p95 при in-flight 4 | 20 мс | 38 мс |
| успешных запросов | 30/30 везде | 30/30 везде |

Длина окна на стоимость не влияет: 50 и 250 мс звука стоят одинаково.

### Предиктор следующего слова (Qwen3-0.6B-Base, Top-20)

| | RU | HE |
|---|---|---|
| inference p50 | 147 мс | 148–151 мс |
| inference p95 | 153–171 мс | 156–169 мс |
| очередь p50 при in-flight 2 | 145 мс | 148 мс |
| очередь p50 при in-flight 4 | 441 мс | 451 мс |

### Критический путь (предиктор + пруннер на окне 150 мс, in-flight 1)

RU: **155 мс** p50 на слово. HE: **164 мс**.

### Бонус: локальный Marian MT (stage1-бенч, тот же под)

| направление | p50 | p95 |
|---|---|---|
| RU→HE | 46 мс | 52 мс |
| HE→RU | 50 мс | 51 мс |

Для сравнения: облачный Gemini-путь в звонке даёт медиану ~1008 мс на перевод.
Качество Marian против Gemini НЕ сравнивалось — это только стоимость.

## Вердикты против целей основателя (50 / 100 / 20 мс)

| проверка | цель | измерено | вердикт |
|---|---|---|---|
| пруннер inference p50 | — | 6–13 мс | **дешевле любого окна** |
| пруннер очередь p95, in-flight 2 | ≤ 20 мс | 6–12 мс | **PASS** |
| пруннер очередь p95, in-flight 4 | ≤ 20 мс | 20 (RU) / 38 (HE) мс | RU на грани, HE **FAIL** |
| предиктор inference p50 | ≤ 50 мс | 147–151 мс | **FAIL, ×3** |
| предиктор inference p95 | ≤ 100 мс | 153–171 мс | **FAIL** |
| предиктор очередь p95, in-flight 2+ | ≤ 20 мс | 152–495 мс | **FAIL** |

## Что это означает

1. **Идея акустического отсечения по стоимости подтверждена.** Проверка
   Top-20 кандидатов по первым 50–250 мс слова стоит 7–13 мс GPU-времени —
   на порядок быстрее самого окна. Стоимость НЕ мешает отсекать кандидатов
   быстрее, чем STT стабилизирует слово. Осталась корректность (нужен
   размеченный корпус — `eval/corpus/`).

2. **Узкое место теперь одно и оно названо: предиктор.** 147 мс на Top-20 —
   это перегенерация коротких лучей; при параллельных запросах растёт только
   очередь (карта не делится). Направления, в порядке дешевизны проверки:
   меньше лучей / короче генерация; батчинг вместо семафора; квантизация;
   модель меньше 0.6B. Любой вариант меряется этим же бенчем за ~$0.10.

3. **Перевод перестаёт быть узким местом цепочки** — 46–51 мс локально против
   ~1008 мс у облака, — если качество Marian переживёт сравнение. Сравнение
   качества — отдельная задача, и она уже в планах (docs/09).

4. Одна 4090 держит **два** параллельных запроса пруннера в бюджете очереди,
   четыре — уже нет (для HE). Для звонка один-на-один этого достаточно.

## Стоимость машинерии на реплику

Слово занимает критический путь ≈155 мс GPU. При $0.34/час это **$0.000015
на слово**, ~$0.0001 на реплику из 7 слов. Стоимость GPU-времени машинерии
пренебрежима против вендорских стадий; экономика упирается в аренду карты как
таковую (простой), а не в запросы.

## Как это было получено (важно для повторения)

Запуск — схемой без SSH (`scripts/runpod-bootstrap.sh`,
`scripts/_runpod_create_nossh.py`): под сам тянет код с нашего сервера,
результаты и живой лог видны через HTTP-прокси пода, отладка — канал патчей.
18 SSH-попыток до этого не дали ни одного числа; эта схема дала полный набор
со второго пода (первый поймал GPU-баг dtype, починенный тем же днём).

---

## Эксперимент 2 (31.08, под `t91wwft77ueslh`): схема основателя «6 вместо 20» и две ручки цены

Гипотеза основателя: жёстко срезать кандидатов — 2 основных + по 2 запасных
= 6 вместо Top-20, при этом кандидаты подбираются из памяти о юзере
(`context_terms` уже в API, стоит 0 GPU-мс — переранжирование).

Сетка: ширина (top_k → num_beams) × длина (max_new_tokens — число
ПОСЛЕДОВАТЕЛЬНЫХ проходов по модели). RU, 30 итераций, один хост.

| top_k | max_new_tokens | inference p50 | p95 | против цели 50 |
|---|---|---|---|---|
| 20 | 6 | 180.8 мс | 190.5 | ×3.6 |
| 6 | 6 | 164.2 мс | 166.5 | ×3.3 |
| 2 | 6 | 165.3 мс | 182.5 | ×3.3 |
| 6 | 3 | 84.1 мс | 91.5 | ×1.7 |
| **6** | **2** | **56.8 мс** | **60.8** | **×1.14 — почти** |
| 2 | 2 | 57.8 мс | 58.9 | ×1.16 |

(Базовая конфигурация на этом хосте — 181 мс против 147 на хосте
`n3uk2ynkko08s9`: между хостами разброс ~20%, сравнивать надо внутри пода.)

**Выводы, оба неожиданно чёткие:**

1. **Ширина почти бесплатна: 20 кандидатов стоят как 2.** 181→165 мс (−9%)
   при срезании 20→2. GPU недогружен, цена шага не зависит от числа лучей
   в этом диапазоне. Следствие для схемы основателя: резать список ради
   СКОРОСТИ не нужно — можно держать хоть 20 и отдавать акустике «2 сначала,
   4 наготове» бесплатной логикой на нашей стороне, не теряя полноту списка.

2. **Вся цена — в длине: ~28 мс за каждый последовательный шаг генерации.**
   6→3→2 токена = 164→84→57 мс, строго линейно. Слово из 1–2 токенов
   предсказывается за 57 мс; шесть токенов вперёд — за 164.

3. До цели 50 мс осталось ×1.14. Следующие рычаги — уже не параметры, а
   реализация: один проход вместо генерации (логиты первого токена дают
   кандидатов сразу), отказ от beam-search-обвязки HF, torch.compile,
   квантизация. 28 мс за проход 0.6B на 4090 — это накладные расходы
   обвязки, а не предел железа.

4. Очередь при 2 параллельных запросах = полная цена запроса (57 мс при
   57 мс инференса) — предиктор по-прежнему не делит карту. Батчинг вместо
   семафора остаётся отдельной задачей.

**Не измерено здесь (по-прежнему):** попадание верного слова в список из
6 против 20 — это вопрос корпуса, не стоимости. Резать список ради скорости
оснований больше нет; резать ради качества отсечения — может быть, но это
решит замер на корпусе.

---

## Эксперимент 3 (31.08, под `b0jxilt07hcur3`): локальный TTS — Chatterbox (MIT) на 4090

Вопрос основателя: посадить «условную Cartesia» рядом с движком, чтобы срезать
сетевой путь и сделать спекулятивный пред-синтез кандидатов бесплатным. Из
открытых движков с ивритом+русским и клонированием голоса коммерчески пригоден
по лицензии фактически один — Chatterbox Multilingual (MIT); сервис для него
уже был в репо (`gpu/tts/app.py`, порт 8104). Встроенный голос, fp32, батч 1,
без оптимизаций. `results/gpu-4090/tts-cost.json`, лог `pod-log-b0jxi.txt`.

| кейс | латентность p50 | длит. аудио | RTF |
|---|---|---|---|
| слово RU («выплата») | 2 998 мс | 2 000 мс | 1.50 |
| слово HE («תשלום») | 1 707 мс | 760 мс | 2.25 |
| фраза RU (48 зн.) | 4 321 мс | 3 480 мс | 1.24 |
| фраза HE | 3 687 мс | 2 840 мс | 1.30 |
| пачка 6 кандидатов RU | 30 404 мс | — | — |
| пачка 6 кандидатов HE | 19 205 мс | — | — |

Для сравнения: Cartesia в звонке — 240–260 мс на всю стадию.

**Вердикт однозначный: в звонок в этом виде — нет.** RTF > 1 на 4090: движок
генерирует МЕДЛЕННЕЕ, чем звучит результат. Слово стоит 1.7–3 с (в 12–15 раз
дороже всей стадии Cartesia), спекулятивная пачка из 6 кандидатов — 19–30 с
при бюджете в сотни мс. Это не «дотюнить»: архитектура авторегрессионная,
полная реплика целиком; fp16/compile дадут разы, а нужны два порядка.

**Куда это годится: мессенджер** (docs/16-messenger.md, отдельный продукт).
Для асинхронного голосового сообщения RTF 1.2–2.2 приемлем, MIT-лицензия и
иврит+русский+клонирование — ровно его профиль. Кандидат для мессенджера
живой; следующий шаг там — качество голоса на слух основателя.

**Для звонка вывод другой:** спекулятивному пред-синтезу нужен движок
< ~100 мс/слово с ивритом и клонированием. Открытого такого сегодня нет.
Cartesia остаётся в звонке; проверить стоит VoxCPM2 (пакетное качество уже
одобрено ухом 24.08, скорость на 4090 отмечалась как высокая — но цену
слова/пачки надо снять этим же бенчем) и Qwen3-TTS для RU-стриминга (иврита
нет — только как половинка пары).

**Не измерено:** качество/похожесть голоса (ухо основателя), цена клон-промпта
(референс-WAV на под не отправлялся — биометрия), стриминговый TTFA (движок
пакетный по определению).

---

## Эксперимент 4 (31.08, под `ql8zh8mgtep12z`): VoxCPM2 — ворота TTFA ПРОЙДЕНЫ

Кандидат, названный основателем следующим. VoxCPM2 (OpenBMB, Apache-2.0,
иврит+русский+клонирование, настоящий стриминг). Встроенный голос, bfloat16,
in-process замер. `results/gpu-4090/voxcpm-cost.json`, лог `pod-log-ql8zh.txt`.

| кейс | TTFA p50 | TTFA p90 | полная генерация p50 | RTF |
|---|---|---|---|---|
| слово RU | 62 мс | 66 мс | 478 мс | 0.43 |
| слово HE | 64 мс | 67 мс | 541 мс | 0.42 |
| фраза RU | 63 мс | 66 мс | 1 003 мс | 0.42 |
| фраза HE | 62 мс | 64 мс | 1 009 мс | 0.42 |
| пачка 6 слов | — | — | 3 072 мс (перекрытие не помогает) | — |

**Ворота из eval/open-hebrew-tts-findings.md: TTFA p90 = 66.8 мс против
≤ 250 мс — PASS с четырёхкратным запасом.** Первый звук приходит быстрее,
чем у Cartesia (TTFB 130–210 мс + сеть), и в 25–45 раз быстрее Chatterbox.
RTF 0.42 — генерирует в 2.4 раза быстрее собственного звучания.

Загрузка модели: 57 с на старт процесса (в т.ч. прогрев torch.compile) —
цена старта, не запроса. Перекрытие двух генераций выигрыша не даёт: движок
сериализуется на карте изнутри.

### Сравнение трёх движков на одном материале

| | Cartesia (звонок) | Chatterbox (MIT) | VoxCPM2 (Apache-2.0) |
|---|---|---|---|
| первый звук | 130–260 мс | 1 700–3 000 мс | **62–67 мс** |
| RTF | стриминг | 1.24–2.25 | **0.42** |
| иврит + клон | ✓ | ✓ | ✓ |
| водяной знак | нет | есть | нет |

### Что это меняет и что осталось

При TTFA 62 мс **пред-синтез кандидатов почти теряет необходимость**: можно
синтезировать только ПОДТВЕРЖДЁННОЕ слово и всё равно отдавать первый звук
через ~60 мс после коммита. Полная пачка из 6 кандидатов (3.1 с) при
пословном темпе не нужна.

**Открытым остаётся ровно то, из-за чего VoxCPM2 был отвергнут 24.08:
качество ПОТОКОВОГО режима на слух** (стриминг отключает встроенный сторож
качества — docs/16). Образцы для суда уха сгенерированы В ПОТОКОВОМ режиме
(`eval/voxcpm-samples/v2/`, вне git): RU — основателю, HE — носителю
(вторая половина ворот, слепой тест). Плюс не измерено: качество клона
(референс на под не отправлялся) и цена префилла на кусок в живом
инкрементальном конвейере.

---

## Эксперимент 5 (31.08, под `g2svakgxjat9cr`): ПЕРВЫЙ ЗАМЕР КОРРЕКТНОСТИ на живом голосе — REJECT

Корпус: 32 фразы `ru-m1` (основатель, диктофон телефона, 31.08), нарезка и
предварительная разметка онсетов — Whisper (помечено в манифесте). Строки
1/12/18 исключены: целевое слово первое, префикса нет (так задумано сценарием).
`results/gpu-4090/replay-ru-m1.{json,md}`, лог `pod-log-g2sva.txt`.

**Вердикт гейта: REJECT.** Предиктор (Qwen3-0.6B-Base, сток):

| recall@1 | @3 | @5 | @10 | @20 |
|---|---|---|---|---|
| 0% | 0% | 0% | 0% | **3.1%** (1 из 32) |

До акустики очередь почти не дошла: в 31 случае из 32 верного слова в списке
не было, и проверять пруннеру было нечего (единственный залёт — окно 250 мс,
удержание 1/1; n=1, ничего не значит).

**Три изъяна ЭТОГО прогона, найденные в его же артефактах, — чинятся до
всякого вывода о модели:**

1. **Префиксы поданы с отрезанной пунктуацией** (мой скрипт нарезки строгал
   хвостовые запятые). «Мне надо, секунду» без запятой читается моделью как
   «надо секунду» — и кандидаты уезжают в «пожалуйста/назад» вместо глаголов.
   Протокол сценария прямо велит: prefix — всё до жирного слова, дословно.
2. **contextTerms не подавались вовсе**, хотя сценарий определяет их для
   каждой строки (тема — 1–2 слова, не совпадающие с целевым). Это штатная
   часть эксперимента, а не читерство — она проверяется кодом бенча.
3. **Фактический список — 7 кандидатов, а не 20** (candidateCount=7): лучи
   схлопываются в немногие уникальные слова. Ширина почти бесплатна
   (эксперимент 2) — надо просить top_k=50, чтобы двадцатка была настоящей.

Перемер с тремя исправлениями — следующий под. Если recall останется около
нуля и после этого, вывод станет содержательным: стоковая базовая модель 0.6B
как угадыватель не годится, нужна инструктивная/дообученная — то самое
«обучаемая модель» из исходной идеи основателя.

Отдельно: качество иврита VoxCPM2 в потоковом режиме — **брак по уху
основателя** (31.08, исправленные 48кГц-образцы). Ворота TTFA остаются
пройденными, ворота качества — нет. Cartesia в звонке остаётся.

### Эксперимент 5б (под `5pzvkwohmcy008`): три исправления НЕ сдвинули recall — вывод о модели

Перемер с починенными изъянами прогона: префиксы дословно с пунктуацией,
contextTerms по протоколу, top_k=50 (фактических кандидатов стало 16–29
вместо 7). **Recall@20 остался 3.1%** (то же единственное попадание —
ru-m1-006 «готовы», ранг 18). `results/gpu-4090/replay-ru-m1-v2.{json,md}`.

Теперь провал — свойство модели, а не методики. Как промахивается стоковый
Qwen3-0.6B-Base (кандидаты из отчёта):

- «Стой, кажется, я …» → не, правильно, понимаю, ничего, так… — среди 16
  нет «забыл»;
- «Билеты на …» → кинотеатр, Олимпийские, велосипеды… — ни одной даты;
- «Я в аптеке, взял, что ты просила, и ещё …» → раз, одну, что-то, 1000… —
  ни одного аптечного слова, буст темы не помог: точных совпадений нет.

Модель продолжает грамматично, но не знает разговорного регистра и не
удерживает бытовую семантику. **Вывод: стоковая базовая 0.6B как угадыватель
не годится.** Это подтверждает исходный тезис основателя: предиктор — место
СВОЕЙ обучаемой модели, а не готовой. Лестница следующих проверок, каждая
меряется этим же конвейером за ~10 центов:

1. инструктивный вариант той же модели + промпт «продолжи реплику одним
   словом» (только код, без обучения);
2. базовая модель крупнее (1.7B) — цена шага вырастет, замерим;
3. дообучение на диалоговых корпусах RU/HE — настоящий путь по docs/15;
4. частотный прио́р из логов звонков поверх LM.

Пруннер на живом голосе основателя: RTT p95 = 7.4–7.9 мс — совпало с
синтетикой (эксп. 1). Цена машинерии подтверждена на настоящей речи; её
корректность ждёт предиктора, который хотя бы иногда попадает.

---

## Эксперимент 6 (31.08, под `heq9zvfnc1lj6y`): лестница моделей × контекст × язык

Текстовый recall (без аудио — дешёвый первый фильтр перед полным replay).
Линейки: RU — 32 фразы корпуса основателя; EN — полное зеркало тех же строк;
ES — срез из 12 (индикативно: фонетика ловушек не переносится, перевод
приближённый). top_k=50. `results/gpu-4090/recall-matrix/`,
`scripts/predictor-recall-bench.py`, линейки в `eval/corpus/linesets/`.

**recall@20 (доля фраз, где верное слово в двадцатке):**

| конфигурация | RU | EN | ES | rtt p50 |
|---|---|---|---|---|
| A: 0.6B-Base, продолжение | 3.1% | 6.3% | 8.3% | 148 мс |
| B: 0.6B-инструктивная, чат | 3.1% | 9.4% | 8.3% | 175 мс |
| C: B + **контекст** (пол/собеседник/тема) | 9.4% | 21.9% | 8.3% | 239 мс |
| D: 1.7B-Base, продолжение | 6.3% | 6.3% | 33.3% | 150 мс |
| E: 1.7B-инструктивная, чат + контекст | **28.1%** | 21.9% | **66.7%** | 337 мс |

На E по-русски: recall@3 = 15.6%, recall@10 = 28.1%, recall@1 = 3.1%.

### Выводы

1. **Лестница работает: с 3.1% до 28.1% на русском без единой минуты
   обучения** — только инструктивная модель, промпт «продолжай реплику» и
   контекст. Рост ×9.
2. **Контекст основателя (docs/15) — сильнейший одиночный рычаг на малой
   модели**: B→C утраивает recall (RU 3.1→9.4, EN 9.4→21.9). Первое
   измеренное подтверждение тезиса о сужении контекста. Память из прошлых
   разговоров пока сымитирована темой строки — настоящей памяти в продукте
   нет, её вклад не измерен.
3. **Языковой перекос — измерен**: на стоковых мелких моделях RU стабильно
   хуже EN/ES (3.1 против 6.3/8.3). Пара RU↔HE требует своей модели не
   из-за красоты идеи, а потому что готовые модели мало видели разговорного
   русского и иврита. ES 66.7% на E — с оговоркой: срез из 12 более простых
   строк.
4. **Цена ступеней**: контекст удлиняет промпт (175→239 мс), 1.7B дороже
   0.6B (283–337 мс против 148–175 при равной длине). E в текущем виде не
   влезает в бюджет 50 мс — сначала добиться попаданий, потом сжимать
   (короче генерация уже работает — эксп. 2; плюс квантизация/компиляция).
5. До продукта далеко (28% @20), но траектория доказана. Следующие ступени:
   4B-инструктивная (потолок без обучения), дообучение на диалогах RU/HE,
   настоящая память о собеседнике. И для попавших в список слов теперь
   имеет смысл полный replay с акустикой — впервые ей есть что проверять.

---

## Эксперимент 7 (31.08, под `0er77yfgatbv8r`): память из «загруженных разговоров»

Настоящих тестеров и памяти нет — по решению основателя разговоры
сфабрикованы: 8 бытовых диалогов пары за неделю (написаны по ТЕМАМ жизни, не
по строкам корпуса), синтезированы клонами Alex+Ellen (Cartesia, 163 с),
прогнаны через STT (транскрипты с настоящими ошибками — «двенадцатое» местами
стало «12»), память — ретрив окна самого релевантного звонка; запрос из
префикса и темы, целевое слово в запрос не входит. Тексты и конвейер —
`eval/corpus/loaded-calls/`; аудио — вне git. Модель E (1.7B-инстр., чат,
контекст), тот же хост для обеих конфигураций.

| | recall@1 | @3 | @5 | @10 | @20 |
|---|---|---|---|---|---|
| E: контекст без памяти (повтор) | 3.1% | 15.6% | 18.8% | 28.1% | 28.1% |
| **F: контекст + память** | **6.3%** | 15.6% | 15.6% | 25.0% | **34.4%** |

(E воспроизвёлся до сотых с матрицей эксп. 6 — методика стабильна.)

**Честный сплит — главное в этом эксперименте:**

| группа | n | E | F |
|---|---|---|---|
| целевое слово ЕСТЬ в памяти (звучало в прошлых разговорах) | 4 | 0/4 | **2/4, оба — ранг 1** |
| целевого слова в памяти НЕТ | 28 | 9/28 | 9/28 |

Выводы:

1. **Память работает ровно там, где обещал механизм продукта:** если слово
   уже звучало у этой пары — оно приходит ПЕРВЫМ кандидатом («подтвердить»
   из вчерашнего «бронь слетает, если не подтвердить»; «батарейки» из
   аптечного списка). Это тот самый эффект docs/15, теперь измеренный.
2. **Как просто дополнительный контекст память ничего не добавляет** (28
   строк без слова: 9→9, ранги шумят в обе стороны) — и удлиняет промпт.
   Значит, в продукте память должна быть ИЗБИРАТЕЛЬНОЙ (ретрив по теме),
   а не «прицепом всей истории».
3. Две строки с словом в памяти всё же не попали (30 «соседки», 32
   «завтра») — потолок пока в модели, не в памяти.
4. Оговорки: диалоги сфабрикованы одним автором; n=4 в ключевой группе —
   направление ясно, величина требует большего корпуса памяти.

---

## Эксперимент 8 (31.08, локально, $0): выравнивание онсетов — и находка хуже, чем ждали

CTC-форс-алайнмент (запасной путь протокола корпуса, чекпойнт пруннера,
смещение записано) на всех 32 строках. Против whisper-меток: **Δ = +55…+175 мс,
медиана +95** — Whisper метит рано, CTC (эмиссия) поздно, истина в коридоре.
Инструменты: `eval/corpus/tools/{align,cut}.py`; манифест v3 несёт обе метки
и компромисс CTC−60 мс.

**Главная находка — не про разметку.** Развёртка поправки (−120…0 мс) плоская:
орфографическое согласие CTC-улики с целевым словом не поднимается выше
8/32 на окне 250 мс и 3/32 на 150 мс **при любой метке**. То есть на живой
телефонной речи (не на синтетическом тоне) читаемость первых 150–250 мс этим
чекпойнтом низкая сама по себе.

Оговорка, которая не даёт закрыть вопрос: проверка была бинарной и
орфографической (совпадение префикса букв), а боевой скорер пруннера ранжирует
нечётко. Настоящий вердикт даст replay v3: retention через rank_candidates на
метках v3. Если и он покажет то же — Stage-2 гипотеза упрётся не в цену
(7 мс, подтверждено), а в **качество акустической улики**, и кандидаты на
замену скорера: wav2vec2-large-ru, фонемные модели, свой лёгкий классификатор
первых звуков (обучаемый — на этом корпусе).

---

## Эксперимент 9 (31.08, под `02shnjn8c66cab`): судейский replay v3 — акустика РАБОТАЕТ на живом голосе

Всё лучшее за день в одной связке: метки v3 (эксп. 8), предиктор
1.7B-чат + контекст + память (проброс contextNote в бенч), боевой нечёткий
скорер пруннера. `results/gpu-4090/replay-v3.{json,md}`.

Предиктор воспроизвёлся: recall@20 = 34.4% (11 строк из 32 дали акустике
работу). **Удержание истины акустикой (по тем 11 строкам, где ей было что
проверять):**

| окно | Top-10 | Top-5 | Top-3 | Top-1 |
|---|---|---|---|---|
| 50 мс | 72.7% | 45.5% | 45.5% | 18.2% |
| 100 мс | 81.8% | 54.5% | 45.5% | 18.2% |
| 150 мс | 72.7% | 45.5% | 36.4% | 18.2% |
| **250 мс** | **90.9%** | **90.9%** | **81.8%** | **54.5%** |

vanished = 0, errored = 0, harmed ≤ 18%. Очередь 0.1 мс, RTT p95 ≤ 21 мс.

**Выводы:**

1. Грубая побуквенная проверка эксп. 8 оклеветала скорер: боевое нечёткое
   ранжирование на тех же метках и том же голосе РАБОТАЕТ. «Охранник» видит —
   слепым был мой тест.
2. Первые числа корректности идеи Stage 2 на живой речи: к 250 мс слова
   истина в топ-5 в 91% случаев и НА ПЕРВОМ месте в 54.5%; уже на 50 мс
   топ-10 держит 73%. В связке с памятью (ранг 1 из прошлых разговоров)
   спекулятивный конвейер впервые виден живым насквозь.
3. Вердикт гейта всё ещё REJECT — честно: узкое место предиктор (34.4%
   против порога 95%), выборка акустики n=11 (< 30), lead vs STT не измерен
   (базы нет — скрипт «не реализовано»), метки без ручной калибровки, один
   диктор.
4. Порядок дальнейшего не меняется: (а) база STT и первый lead; (б) предиктор
   (дообучение — главная ступень); (в) ручная калибровка меток; (г) женская
   и ивритская дорожки.
