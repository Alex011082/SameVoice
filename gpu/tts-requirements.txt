# GPU 1 / SPEAK A/B runtime. Kept separate from GPU0 because TTS packages may
# pin different torch/transformers versions.
fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
chatterbox-tts>=0.1.7,<0.2
# numpy<2 НЕ по вкусу, а по требованию chatterbox-tts 0.1.7: он объявляет
# numpy>=1.24,<2, и при numpy>=2 сборка падает на нерешаемых зависимостях
# (прогон 33329357251, 30.08.2026). Держим отдельный venv /opt/venvs/speak
# ровно ради таких расхождений: у GPU0 numpy остаётся своим.
numpy>=1.24,<2
